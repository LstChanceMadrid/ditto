import { screenHeight, screenWidth } from "../../../constants/constants";

export const homeConstants = {
    moveUpConstant: () => { 
        if (this.props.sprite.y > screenHeight/12) {
            
        } else {
            return
        }
    }
}